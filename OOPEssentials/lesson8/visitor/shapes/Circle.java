package lesson8.visitor.shapes;

public class Circle extends Dot {
    private int radius;

    @Override
    public void move(int x, int y) {
        super.move(x, y);
    }

    @Override
    public void draw() {
        super.draw();
    }
}
